<script>
// ?????????? ???????? ???????????? ???????
// ???? ??????? ???????
var pr = true;
while ( pr ) {
    alert ('???? ??????? ???????.');
    var n = 0;
    n = vvodN(1);
    if ( n <= 1 ) {
        alert ('?????? ??????? ?.?. ?????? 1.');
    }
    else { pr = false; }
}
// ???? ??????? ?? ???????
var str = 1; // ??????? ?????
var matr = new Array(n);
for(var i = 0; i < n; i++) {
    matr [i] = new Array(n);}
for ( var i = 0; i < n; i++ ) { 
    alert ('???? ?????? ' + str);
    for ( var j = 0; j < n; j++ ) {
       matr [i][j] = vvodN(1);  
    }
    str = str + 1;
}
alert ( matr );  

//?????????? ??????? ? ???????????? ????.
var b = 0; 
var sch = 0;// ??????? ???????? ?????
i = 0;
do { 
    if ( matr [i][i]  !== 0 ) {
        for ( j = i + 1; j < n; j++ ) { 
               b = matr[j][i]/matr[i][i]; 
             for ( var k = 0; k < n; k++) { 
                 matr[j][k] = matr[j][k] - matr[i][k] * b; 
             }
        } i = i + 1;
    }
else { var l = 1; // ??????? ?????
    do { sch = sch + 1;
        for ( j = i; j < n; j++) {
            b = matr [i][j]; matr [i][j] = matr [i+l][j]; matr [i+l][j] = b;
        }  
        l = l + 1;  if ( l > n-i-1 ) { alert ( '???????????? ??????? ?????     0.');}
     }
     while ( matr [i][i] === 0);
    }
}
while ( i < n );
alert ( matr );
var det = 1;
for ( i = 0; i < n; i++) { det = det * matr [i][i];}
det = det*st(sch);
alert ('???????????? ??????? = '+ det);
// ???????
// ???? ?????
function vvodN(b) {
    var a = b; 
    var i = true;
do 
    {
        a = prompt('??????? ?????');
        if (isNaN(a)) 
        {
            alert ('?? ????? ?? ?????');
            i = true;
        }
        else 
        {
            a = Number(a);
            return (a);
        }
    }
    while (i); 
}
// (-1)  ? ??????? n
function st(n) {
    var res = 1;
    if ( n % 2 === 0 ) { res = 1;}
    else { res = -1;}
    return res;
}
</script>